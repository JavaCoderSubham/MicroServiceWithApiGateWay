package com.microservice.configuration;


//@Configuration
//@EnableWebSecurity
public class GateWaySecurity {
	
	/*
	 * @Bean public SecurityFilterChain securityFilterChain(HttpSecurity http)
	 * throws Exception { return http.csrf().disable() .authorizeHttpRequests()
	 * .anyRequest() .authenticated() .and() .httpBasic() .and() .build(); }
	 * 
	 * @Bean public UserDetailsService userDetailsService(PasswordEncoder encoder) {
	 * 
	 * UserDetails user1 = User.withUsername("subham")
	 * .password(encoder.encode("subham")) .roles("USER") .build();
	 * 
	 * return new InMemoryUserDetailsManager(user1); }
	 * 
	 * @Bean public PasswordEncoder passwordEncoder() { return new
	 * BCryptPasswordEncoder(); }
	 */

}









